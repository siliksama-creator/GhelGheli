-- ═══════════════════════════════════════════════════════════════════════
-- ۱۰۵ — نمایشِ فهرستِ جوایزِ لیگ، قبل از اینکه کسی سکه ببرد
--
-- خواسته: اگر هنگامِ ساختِ لیگ تیکِ «نمایش لیست جوایز» بخورد، وب و
-- اندروید تا وقتی هنوز کسی در آن لیگ سکه نبرده، جایزهٔ هر رتبه را نشان
-- بدهند. پیش‌فرض خاموش است تا لیگ‌های قبلی بی‌اجازه فهرستشان را لو ندهند.
-- ═══════════════════════════════════════════════════════════════════════

ALTER TABLE league_seasons
  ADD COLUMN IF NOT EXISTS show_prize_list BOOLEAN NOT NULL DEFAULT false;

COMMENT ON COLUMN league_seasons.show_prize_list IS
  'اگر true باشد، تا وقتی کسی در این لیگ سکه نبرده، فهرست جوایز رتبه‌ها در وب و اندروید دیده می‌شود.';
