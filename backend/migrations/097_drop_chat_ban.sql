-- حذف کامل سیستم بن چت (تصمیم مالک، ۱ مهر ۱۴۰۵).
-- کاربران فقط پیام آماده/استیکر می‌فرستند؛ بن لازم نیست.
-- مسیر ادمین (PATCH /admin/chat/users/:id/ban)، چک‌های REST/سوکت و دکمهٔ پنل
-- در همین کامیت از کد حذف شدند؛ این مایگریشن ستون را از دیتابیس برمی‌دارد.
-- ردیف‌های تاریخی audit (ban_chat_user) و اعلان‌های chat_penalty دست‌نخورده می‌مانند.
ALTER TABLE users DROP COLUMN IF EXISTS chat_banned_until;
