services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ghelgheli
      POSTGRES_USER: ghelgheli
      POSTGRES_PASSWORD: ghelgheli
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
