// فلگِ زمان‌بیلد — «آیا پنل ادمین در این خروجی گنجانده شود؟»
//
// ═══════════════════════════════════════════════════════════════════════
// تصمیم: اپِ منتشرشده فقط کاربری است
// ═══════════════════════════════════════════════════════════════════════
//
// مدیریت پروژه فقط از **پنل وب ادمین** انجام می‌شود (`admin/`). اپ موبایلی که
// در استور/توزیع عمومی می‌رود نباید هیچ کدِ ادمینی را کامپایل کند:
//   • سطح حملهٔ ۳۱ صفحهٔ مدیریتی + مسیر احراز ادمین از اپِ عمومی خارج می‌شود.
//   • فقط یک منبع حقیقت برای مدیریت می‌ماند (ادمین وب) و دو کلاینت لازم نیست
//     هم‌گام نگه داشته شوند.
//
// چون مقدار `const` است، کامپایلر AOT در بیلد پیش‌فرض (که مقدار `false` است)
// کل درختِ پوستهٔ ادمین را مرده می‌بیند و **tree-shake** می‌کند — حتی اگر
// کدش در آینده دوباره به مخزن برگردد.
//
// احتیاط/آینده: اگر روزی یک «اپ ادمینِ داخلی» خواستید، صفحات را از تاریخچهٔ
// گیت (قبل از کامیتِ حذف) برگردان و بیلد را با
//   flutter build apk --dart-define=INCLUDE_ADMIN=true
// بساز. خروجی انتشاری هیچ‌وقت این فلگ را روشن نمی‌کند.
//
// جزئیات کامل در `docs/ADMIN_PANEL_MOBILE_RETIREMENT.md`.
const bool kIncludeAdmin = bool.fromEnvironment('INCLUDE_ADMIN');
