node_modules/
.env
.DS_Store
*.log
mobile/.dart_tool/
mobile/build/
mobile/android/
mobile/ios/
mobile/linux/
mobile/macos/
mobile/windows/
admin/dist/
backend/uploads/
apk/

# Build output (built on the server / in CI, never committed — a tracked
# dist/ makes `git pull` conflict on the VPS every deploy).
userweb/dist/
