# نقشه‌راه — یکپارچه‌سازی کامل: پنل زنده، متن‌های زنده، اپ بدون آپدیت

تاریخ: ۳ سپتامبر ۲۰۲۶ · روی پایهٔ کامیت `f2daf1d` · این فایل **سندِ مرجعِ کارِ جاری** است
(پیش از این فقط در گفت‌وگو بود و در هیچ جا ذخیره نشده بود — همین باعث شد یک جلسه
دربارهٔ «فازها» حدس زده شود.)

> **راهِ تیک‌زدن:** هر بندِ انجام‌شده با ✅ و کامیتِ مرجع علامت می‌خورد.
> علامت ⬜ یعنی انجام‌نشده. `deploy` هر فاز جدا و با rollback خودکار است.

---

## ۰) وعدهٔ نهایی

| وعده | معنی دقیق |
|---|---|
| هر متن و عددی که کاربر می‌بیند، از پنل قابل تغییر است | وب کاربر + اپ اندروید — همه از یک منبع |
| تغییر = لحظه‌ای، بدون نصب | ذخیره در پنل → در اجرای بعدی کلاینت دیده می‌شود |
| اپ فقط برای «شیء جدید» آپدیت می‌شود | بازی/صفحه/ساختار UI تازه. هیچ عدد/متن/قانونی دلیلی برای آپدیت نیست |
| دو پنل ادمین (وب و اندروید) یک‌رنگ‌اند | همان منو، همان صفحه‌ها، همان API، همان اعتبارسنجی |
| زبان ساده برای ادمین غیرفنی | منوی گروه‌بندی‌شده، توضیح یک‌خطی، پیش‌نمایش زنده، واپس‌گیری |
| ظاهر اپ کاربری دست نمی‌خورد | فقط «درِ داده‌ها» عوض می‌شود |
| پیامک/OTP لمس نمی‌شود | روی حالت تست می‌ماند |

## ۱) وضعیتِ امروز (لحظهٔ نوشتنِ نقشه‌راه)

سالم و زنده (دست‌نخورده): `app_settings` + کشِ همگامِ `opsConfig`، دروازهٔ `GET /api/config`،
پنل‌های ادمین (۲۲ بخش در هر کلاینت)، خواندنِ همان API توسط هر دو کلاینت.

مانده‌ها: دستهٔ الف (۱۰ عددِ ساختاری هاردکد)، دستهٔ ب (۵ متنِ کهنه با عددِ زنده)،
و ۸ خطای واقعی (بخش ۴).

## ۲) معماری هدف — «یک کتاب، سه خواننده»

```
app_settings (تنها منبع حقیقت) + live_copy + live_rules
   ├── پنل ادمین وب  ──┐
   ├── پنل ادمین اندروید ──► همان API (PATCH + preview + history + revert)
   └── GET /api/config ──► AppConfig (اندروید) / liveConfig (وب) ──► متن و عددِ هر صفحه
```

سه قاعدهٔ غیرقابل‌انحنا:
1. هیچ رقمِ «بازاری» در متن UI کلاینت نباشد — از config یا از قالبِ `live_copy`.
2. هیچ عددِ کاری در کدِ کلاینت نباشد مگر **فول‌بکِ ایمن** برابرِ مقدارِ فعلیِ محصول.
3. هر مقدارِ تازه: اول در API، بعد در کلاینت (کلاینتِ قدیمی کلیدِ ناشناخته را نادیده می‌گیرد).

مکانیزمِ «لحظه‌ای»: prime کردنِ کش هنگام ذخیره · fetch در هر اجرا + بازگشت به پیش‌زمینه ·
`configVersion` برای دیباگ · `live_content_history` (۲۰ نسخه) + دکمهٔ «واپس‌گیری».

## ۳) فازها

### فاز ۰ — رفع خطاهای پیدا شده — ✅ کامل و مستقر (`1bc7215`)
- ✅ شمارندهٔ جوایز «/۳۰»→«/۵۰۰» در پنل اندروید (`admin_rewards.dart`)
- ✅ یکدست‌شدنِ کد اتاق (ساکت + عمق‌لینک، ۴ رقم عدد) در `engine.js`
- ✅ پاک‌کردنِ باقی‌ماندهٔ تست از دیتابیسِ عرضه (کاتالوگ ۵۶ طرحی دست‌نخورده)
- ✅ README: «۳۰ جایزه»→«۵۰۰»
- ✅ سند تحویل: بلوک «به‌روزرسانی ۳ سپتامبر»
- ❌ رد شد: «هدیهٔ عضویت فقط در پنل وب» — مثبتِ کاذب؛ پنل اندروید دارد

### فاز ۱ — زیرساخت `live_copy` + `live_rules` — ✅ کامل و مستقر (`0cf754c`)
- ✅ مایگریشن `082_live_content.sql` (`live_content_history`)
- ✅ سرویس `liveContent.js` (الگوی opsConfig: کشِ همگام + preload + prime)
- ✅ `live_rules` با بازهٔ امنِ برحسبِ واقعیتِ کد (`memoryPairs` ۴–۸ نه ۴–۱۶)
- ✅ `live_copy` با قالبِ همهٔ گروه‌ها + `COPY_CONTRACT`
- ✅ اتصالِ واقعیِ ۵ عدد به منطق (جفت‌یاب، اتصالِ دوباره، طول کد اتاق، تیکت، چرخشِ معرفی)
- ✅ `GET /api/config` → `copy`/`rules`/`configVersion` + `referral.spinsPerDailyThreshold`
- ✅ endpointها: `GET|PATCH /admin/settings/live-content/*` + `preview` + `history/:key` + `:key/revert`
- ✅ `testLiveContent.js` (۵۲ نگهبان)
- ⚖️ `DECK_SIZE` دوئل ثابت ۵ ماند (پنج `ROUND_FOCUS` = طراحیِ بازی + UI انتخاب ۵ کارت)
- ⚖️ ساعتِ ریستِ گردونه عددِ قابل‌تنظیم نیست (منطقِ نیمه‌شبِ تهران)؛ فقط لحنِ جمله زنده است

### فاز ۲ — اتصالِ کلاینت‌ها — ✅ کامل (بدهیِ مستند: یک کلیدِ RichText، بند «پاکسازیِ فول‌بک»)
- 🟨 **۲.۱ Providerِ متمرکزِ config**
  - ✅ وب: `userweb/src/lib/liveConfig.js` (کشِ ماژول‌سطح + `useLive()`)؛ `main.jsx` یک‌جا prime می‌کند،
        fetchهای تکراریِ `League.jsx` و `games.jsx` حذف/ادغام شد (۴ نقطه → ۱ منبع)
  - ✅ اندروید: `mobile/lib/core/app_config.dart` (`AppConfig` تک‌نمونه + `liveText`/`liveRule`). **fetch تازه‌ای اضافه نشد**: `home_shell.dart` همان بدنه‌ای را که از قبل می‌گرفت با `apply()` به منبع می‌دهد، و `ensure()` در `main.dart` هم چون پنجرهٔ ۱.۲ ثانیه‌ایِ `ApiClient` با درخواستِ پوسته ادغام می‌شود؛ بازگشت از پس‌زمینه یک `refresh()` می‌زند (`WidgetsBindingObserver`، همان الگوی `MemoryGuard`). دلیلِ این طراحی: **هر ۷ نقطه‌ی برداشت، همان بدنه را با `AppConfig.instance.apply()` به منبع می‌دهد** (گردونه، بازی‌ها، لیگ، ضربه‌زن، دیالوگِ اتاق، ثبت‌نام، پوسته) — گاردِ `testLiveWiring` نقطه‌ای که fetch می‌زند و apply نمی‌کند را قرمز می‌کند. `mobile/test/home_shell_test.dart` تعدادِ درخواستِ هر مسیر را می‌شمارد (بیش از ۲ = قرمز) و `auth_screen` با `fresh: true` کش را رد می‌کند — یک fetch سراسریِ نو همان تست را می‌شکست.
- ✅ **۲.۲ اتصالِ موردی** (وب و اندروید — هر گروهی که در `DEFAULT_COPY` است، در هر دو کلاینت خوانده می‌شود؛ گاردِ «کلیدِ بی‌مصرف» سقفش روی ۱ است و آن یک هم `coinGuide.lead` است که پایین توضیح داده شده)
  - ✅ راهنمای سکه: جمله‌ها از `coinGuide.*`، ستون‌ها از `stakeLabel`، ورودی‌ها از `dailyCoinQuota`
  - ✅ نوارِ نرخ: `botNote`/`privateNote`/`carryover*`
  - ✅ پلاس: `plus.monthlyBadge` / `plus.annualBadge` از `days`/`savingPercent`ِ خودِ پلن
  - ✅ ضربه‌زن: «۵۰ لول» → `games.tapSubtitle` با `{levelCount}` از `tapLevelCount`
  - ✅ دعوت: «۱» → `referral.dailySpinRule` (وب: Referral + Wheel)
  - ✅ گردونه: `wheel.resetNote`
  - ✅ جفت‌یاب: `games.memoryRule` + `memoryPairs`
  - ✅ کد اتاق: `games.roomCodeLabel` + `roomCodeLength` (با maxLength)
  - ✅ بازگشت شبکه: `reconnect.offlineNote` + `reconnectSeconds`
  - ✅ آواتار: `avatars.countLabel` + تعداد از `/api/config.avatars` / `GET /api/avatars`
  - ✅ استریک: `streak.cycleDone`/`streak.cycleNext` در اندروید و `streak.webDone` در وب. طولِ چرخه از تعدادِ ردیف‌هایِ پاداش (`streak_settings.rewards`) می‌آید و دیگر هیچ ۷ِ سفت‌شده‌ای نمانده: تیتر، نوارِ پیشرفت، `clamp`‌ها، گره‌ها و `isLastDay`. سه باگِ واقعی همین‌جا پیدا و رفع شد: با چرخهٔ ۱۰ روزه سه روزِ آخر اصلاً رسم نمی‌شد، `isLastDay: i == 6` جایزهٔ طلایی را به روزِ هفتم می‌چسباند، و `100 + i * 50` جای داده‌هایِ نرسیده را با **جایزهٔ ساختگی** پر می‌کرد.
  - ✅ دوئل و جفت‌یاب: `games.duelSubtitle` و `games.memorySubtitle` در هر دو کلاینت. `DECK_SIZE` دوئل همچنان ۵ ثابت است — طبقِ بندِ قطعیِ نقشه‌راه فقط **متن** زنده شد.
  - ⬜ گذر: یکپارچه‌کردنِ فول‌بک‌های پراکندهٔ قرص‌ها در یک نقطه
  - ✅ پشتیبانی و منشور: `support.privacyTitle`، `support.privacySections`، `support.ticketRule`، `support.attachmentsFull` در هر دو کلاینت. این دور **سه نقصِ واقعی** نهفته بود، نه فقط «سیم‌نکشیده»:
    • سقفِ پیوست در وب سفت بود (`const MAX_SHOTS = 5`) ولی اندروید آن را از `quota` می‌خواند — ادمین ۳ می‌گذاشت، کاربرِ وب ۵ تا انتخاب می‌کرد و سرور 413 می‌داد. حالا هر دو از `live_rules.maxTicketAttachments` (فول‌بک ۵ = همان عددِ امروز).
    • منشورِ وب **کوتاه‌تر** از اندروید بود: «بخت‌آزمایی» نداشت و به‌جای «قوانین رسمی بانک مرکزی» نوشته بود «قوانین شاپرک و پایا». اختلافِ حقوقیِ پنهان بین دو پلتفرم — با وصل‌شدنِ وب به سرور بسته شد. فول‌بکِ وب عمداً همان متنِ کوتاهِ دیروز ماند تا در قطعیِ شبکه هیچ چیز عوض نشود.
    • دونقطهٔ انتهای تیترها در چیدمانِ کلاینت بود (`<b>{title}:</b>`) و اندرویدش نداشت؛ به خودِ رشتهٔ سرور منتقل شد (قالب نباید واژه‌ای را که کلاینت می‌سازد تکرار کند).
    • `plus.benefitsNote` هم در اندروید داخلِ رشته سفت بود («۳۰ روز»): عدد حالا از `durationDays` خودِ پلن می‌آید. شاخهٔ `annual` عمداً وصل نشد — جمله‌اش فقط در دیالوگِ اندروید هست و در وب وجود ندارد؛ در `EXEMPT_ONE_SIDE` مستند شد.
    • `AppConfig.copySection(group, field)` اضافه شد (خواندنِ **فهرستِ اشیاء**). بی آن، هر محتوای فهرست‌محور مجبور بود در کد بنویسد و «از پنل» به‌نظر برسد. `null` (config نرسیده) از `[]` (ادمین صراحتاً خالی کرد) جداست.
    • `coinGuide.lead` تنها بدهیِ مانده است و **عمدی**: در وب با `<b>` و در اندروید با `RichText/TextSpan`ِ رنگی رندر می‌شود؛ وصل‌کردنش بولدِ انتخابی را می‌سازد = تغییرِ طرح = خطِ قرمز. تا وقتی پنل «فیلدِ فقط‌نمایشی» ندارد، در گارد به‌عنوانِ بدهیِ مستند ثبت است (نه حذفِ ردیف، که ادمین فکر کند کلید غیب شده).
  - ✅ بررسی عکس: `photoReview.pendingNote` + `live_rules.reviewSlaHours` در هر دو کلاینت (وب در `title`ِ چیپ، اندروید در SnackBar) — وعدهٔ «۲۴ ساعت» دیگر در هیچ‌کدام نوشته نشده.
- ✅ **۲.۳ نگهبان‌ها**
  - ✅ بازطراحیِ `userweb/tool/coin-parity.mjs`: رقمِ هاردکد را نمی‌سنجد، **کلیدِ live_copy** و
        ساخته‌شدنِ عدد از config را می‌سنجد (گاردِ کهنه خودش مصداقِ «تستی که عددِ منسوخ را قفل می‌کرد»)
  - ✅ `userweb/tool/live_copy_parity` (گاردِ اصلی، ۴۹ سنجه — سه سنجهٔ تازه:
        «فول‌بک نباید `{placeholderِ}` ناپر‌شده داشته باشد» — این را روی خودم گرفتم: fallback را «… {ticketsPerDay} …» نوشته بودم و فقط در آفلاین روی صفحه می‌آمد،
        «کلیدِ بی‌مصرفِ پنل از سقفِ ۱ بیشتر نشود» — که اولین اجرا سه کلید را قرمز کرد و معلوم شد دوتاش **کارِ جعلیِ خودِ گارد** بود (فهرست‌خوانی با `rawList`/`copySection` شمرده نمی‌شد؛ حالا یک شمارشگرِ جدا دارد)،
        و «خواندنِ فهرستی هم شمرده شد» تا همان کوریِ گارد دوباره برنگردد.) و `backend/scripts/testLiveWiring.js` که همان را از بک‌اند هم اجرا می‌کند و سه چیزِ ساختاری را اضافه می‌سنجد: ثبت‌شدنِ گارد در `npm test`ِ هر دو بسته، کامل‌بودنِ بدنهٔ `/api/config` (`copy`/`rules`/`configVersion`/`economy`/`stakes`/`avatars`/`spinsPerDailyThreshold`)، و اینکه هیچ نقطه‌یِ تازه‌ای `/api/config` را مستقل نمی‌زند. گاردِ رقمِ هاردکد در `coin-parity` هم بازنویسی شد: حالا **نامِ جای‌نگهدارها** را با `COPY_CONTRACT` می‌سنجد، نه رقم‌ها را.
  - ✅ قرارداد به‌روز شد: قالبِ `coinGuide.quota` دیگر «ورودی {stakeLow}» نمی‌نویسد؛ برچسبِ لایه از `coinGuide.stakeLabel` ساخته می‌شود و به‌عنوان `{stakeLowText}` تزریق می‌گردد. باگِ واقعیِ پیداشده: نوارِ نرخ «ورودی 100» با رقمِ لاتین می‌داد و کارتِ راهنما «ورودی ۱۰۰» — و گاردِ قدیمی چون فقط **بودنِ** کلید را می‌سنجید، سبز بود.
  - ✅ `docs/live-config-contract.md`: جدولِ کلید → جای‌نگهدار → منبعِ عدد، به‌همراه فهرستِ استثناهایِ طرح (چیزهایی که عمداً در یک کلاینت خوانده نمی‌شوند تا ظاهر عوض نشود) و قانونِ «هر عددی که جا را می‌سازد نه فقط هر عددی که در متن است».

### فاز ۳ — بازسازیِ پنل‌های ادمین — ✅ (۳.۱، ۳.۲، ۳.۴ کامل؛ ۳.۳ الگو‌سازی شد)
- ⚖️ ظاهرِ منو در APKِ نصب‌شده تا بیلدِ بعد عوض نمی‌شود (بیلدِ APK عمداً فقط با
      درخواستِ مالک ساخته می‌شود)؛ کدش در مخزن است و هیچ رفتاری باز نمی‌ماند.
- ✅ ۳.۱ صفحهٔ «متن‌های زنده» در **هر دو** پنل: `admin/src/pages/live-copy.jsx` (وب) و
      `mobile/lib/screens/admin/admin_live_copy.dart` (اندروید). **این ✅ با CI تأیید شده است، نه
      با گاردِ متن**: `analyze-and-test` روی `c6f9bcf` سبز شد (پنج شغلِ workflow، همگی success).
      مسیرِ رسیدن به این خط سه دور طول کشید و هر سه خطایِ واقعیِ خودش را داشت — که هر سه
      به گارد اضافه شد، تا «باگِ همین صفحه» به «قاعدهٔ کل پروژه» تبدیل شده باشد:
      `SectionHeader` از فایلِ اشتباه import شده بود، `_preview`/`_ruleValues` بی مصرف بودند،
      و `await _loadHistory()` (که خودش رفعِ `unawaited_futures` بود) پیامِ موفقیت را بی محافظ کرد.
      سه‌تاییِ خواسته‌شده ساخته شد، ولی این دور یک ✅ اشتباه هم داشت: پنلِ اندروید را با ✅ نوشته بودم
      در حالی که **هرگز با یک SDK واقعی کامپایل نشده بود**؛ `analyze-and-test` شش خطا داد
      (`SectionHeader` از فایلِ اشتباه import شده بود، دو `unused_field`، سه `unawaited_futures`).
      قاعدهٔ تازه‌ای که از همین دور عوض می‌شود: هیچ «پنل ساخته شد»ی ✅ نمی‌خورد تا
      `analyze-and-test` روی همان commit سبز شود — گاردهای متن‌محور، فایل را *متن* می‌خوانند
      و از نشناختنِ یک کلاس خبر نمی‌دهند.
      • خطِ «در اپ: …» (خروجیِ پرشدهٔ سرور زیرِ هر فیلد) حالا در **هر دو** پنل هست؛ در اندروید
        `_preview` فقط پر می‌شد و هیچ‌جا چاپ نمی‌شد — یعنی همان چیزی که analyzer قرمز کرد و
        گاردِ «endpoint صدا زده می‌شود» نمی‌توانست بگیردش.
      • اگر سرور عددی را clamp کند، کادرِ عددیِ اندروید با مقدارِ *نهایی* هماهنگ می‌شود
        (`_syncNums`)؛ قبلاً پاسخِ PATCH در `_ruleValues`ی خاموش می‌نشست (منبعِ حقیقتِ دوم).
      • **پیش‌نمایشِ زنده**: `POST …/preview` — و چون `preview()` حالا خودش جای‌نگهدارها را با
        `live_rules`ی امروز پر می‌کند، پنل *بیِ هیچ fetchِ دیگری از /api/config* عددِ واقعیِ
        کاربر را نشان می‌دهد (قبلاً باید `vars` را خودش می‌فرستاد؛ فراموشیِ یک عدد = فیلدِ خالی
        در پیش‌نمایش = «پنل خراب است»).
      • **بازگردانیِ آخرین تغییر** (`POST …/copy/revert`) و **بازگشت به پیش‌فرضِ کد**
        (`GET …/defaults` → پرکردنِ فرم، بدونِ ذخیرهٔ خودکار؛ دکمهٔ یک‌کلیکه‌ای که مستقیم
        محصول را عوض کند در پنلی که «اشتباهِ مدیر» در آن فاجعه است، طراحیِ بد است).
      • **قفلِ ذخیره**: اگر مدیر `{days}` را از جمله پاک کند، همان لحظه هشدار می‌گیرد و
        دکمهٔ ذخیره تا رفعش کار نمی‌کند — وگرنه باگ *ساکت* است: عدد دیگر هیچ‌وقت نمی‌نشیند.
- ✅ ۳.۲ گروه‌بندیِ منو — **انجام شد** (۲۳ قلم → ۸ گروه؛ نه ۲۲ و ۷: آن عدد
      از شمارشِ دستیِ متنِ گفت‌وگو آمده بود و با `NAV` واقعی یکی نبود — دقیقاً
      همان «مستندِ کهنه» که فاز ۰ برای کشتنش نوشته شد، و این‌بار عددِ *خودِ
      نقشه‌راه* را اصلاح کردیم).
      • ترتیبِ **درونِ هر گروه** عمداً همان ترتیبِ قبلی مانده: عضلهٔ حافظهٔ مدیر
        («فلان صفحه کجا بود؟») نباید با یک refactor بی‌کار شود. فقط دسته‌بندی
        اضافه شده، نه جابه‌جاییِ قلم‌ها.
      • نامِ گروه‌ها «تکنیکال» نیست: «پیکربندیِ متن و اپ»، نه `config`. و
        **تنها در کلاینت‌ها** زندگی می‌کنند، نه در `live_copy` — چون سرتیترِ منو
        «متنِ محصول» نیست که مدیر بخواهد هر روز عوضش کند؛ یکی‌به‌یکیِ واژه‌ایِ
        دو پنل را گاردِ parity تضمین می‌کند.
      • وب: عضوِ ششمِ هر ردیفِ `NAV` = کلیدِ گروه + جدولِ `NAV_GROUPS` در
        `admin/src/main.jsx`؛ رندر در `components/app-shell.jsx` که *یک* نوارِ
        کنار دارد و هم دسکتاپ و هم کشوی موبایل از آن می‌خورند (پس یک ویرایش،
        دو جا). اندروید: سه فهرستِ تازه در `admin_shell.dart`
        (`_adminNavGroups` هم‌شاخصِ چهار فهرستِ موجود + `_groupLabels`) و
        سرتیترها در `_AdminNavList` که کشو و ریلِ ۲۴۸پیکسلی را با هم می‌سازد.
      • رنگِ سرتیتر: `--gg-text-soft` در این پنل **تعریف نشده بود** و فقط
        fallbackِ سفیدِ ۵۵٪ کار می‌کرد (کنتراست ~۲.۸). حالا
        `color-mix(--gg-text-muted 55%, #fff)` است — همان درسی که `.topbar-sub`
        از ممیزیِ کنتراست گرفت: متنِ ۱۱pxِ کم‌رنگ، در این پنل «قربانیِ مجاز» نیست.
      • گاردِ `userweb/tool/admin-copy-parity.mjs` از ۳۰ سنجه به **۴۰** رسید:
        شمارشِ پنج فهرستِ اندروید، تطبیقِ مو‌به‌موِ ترتیبِ گروه‌ها با `NAV`ِ وب،
        برابریِ واژه‌به‌واژهٔ نامِ فارسیِ گروه‌ها، «هر گروهِ مصرفی نام دارد» و
        «هر نامِ جدول مصرف می‌شود» (سرتیترِ مرده)، به‌علاوهٔ سنجه‌هایِ تازهٔ
        `app.bazaarPackage` در تنظیماتِ *هر دو* پنل. `testCardBoxAdmin.js` هم که
        شمارشِ توضیح‌ها را می‌کرد، به قالبِ سه‌سطریِ `NAV` سازگار شد (سه تلاشِ
        regex‌پیچیده سه عددِ غلط داد؛ دو قاعدهٔ ساده درست از آب درآمد).
- 🟨 ۳.۲ِ **باقی‌مانده**: ظاهریِ منو در APKِ نصب‌شده روی گوشی تا بیلدِ بعد عوض
      نمی‌شود (کدِ دارت در مخزن است؛ بیلدِ APK عمداً فقط با درخواستِ مالک ساخته
      می‌شود). هیچ رفتاری در آن باز نمی‌ماند — فقط دسته‌بندیِ بصری.
      `userweb/tool/admin-copy-parity.mjs` (۳۰ سنجه) که NAVِ وب و چهار فهرستِ هم‌شاخصِ
      اندروید (`_pages`/`_titles`/`_icons`/`_subtitles`) را مقایسه می‌کند، نامِ گروه‌هایِ فارسی
      را واژه‌به‌واژه می‌سنجد، مطمئن می‌شود پنل‌ها **همهٔ** گروه‌های `DEFAULT_COPY` را نشان
      می‌دهند (۱۰۰٪ coverage) و یکی‌درمیان‌بودنِ «پیش‌نمایش/بازگردانی/پیش‌فرض» را قرمز می‌کند.
      ثبت‌شده در `npm test` بک‌اند (`testAdminCopyParity.js`) و `test:admin-copy-parity` وب.
- 🟨 ۳.۳: الگویِ «مقایسهٔ یکی‌به‌یکی» برای این صفحه با گاردِ استاتیک ساخته شد (بدِ selenium،
      بدِ سرورِ درحال‌اجرا) و قابل تعمیم است — و در ۳.۲ برای **کلِ منو** (۲۳ قلم، پنج فهرست
      و دو جدولِ نام) مصرف شد. نسخهٔ کاملِ ۲۳ صفحه هنوز ⬜.
- ✅ ۳.۴ برای «متن‌های زنده»: برچسبِ هر فیلد **خودِ جمله** است (تا ۴۶ کاراکتر)، نه نامِ کلید؛
      «حالتِ حرفه‌ای» کلیدهایِ فنی را روشن می‌کند (گزارش‌بهتیم = همان نام‌ها)، هر عدد با
      توضیحِ «چه چیزی را در بازی لمس می‌کند» و بازهٔ مجازش آمده، و ذخیره با هشدار قفل است.
      سقفِ OTP و نرخِ ورود عمداً در این صفحه نیستند: فقط‌خواندنی‌اند و هیچ کلاینتی متنشان را
      از پنل نمی‌خواند.
- ✅ **۳.۴ روی همهٔ صفحاتِ دارایِ ورودی** (`d29acec`، `ef86d38`، `0f61455`، `a223dc3`،
      `4b15afa`، `2e553ea`، `18af495`). مرزی که اینجا کشیده شد
      مهم‌تر از خودِ کار است: توضیحِ فیلدهایِ *پنل* رشتهٔ محصول نیست که به
      `live_copy` برود (ادمین «متنِ پنل» را از پنل نمی‌خواهد)، پس مستنداتِ
      درونِ کد می‌ماند؛ و *همان متن* در هر دو پنل یکسان است — نه با کپیِ
      دستی: توضیح‌هایِ اندروید از خودِ `engine.jsx` خوانده و درج شدند، چون
      «یکی‌به‌یکی» با دست‌نویسیِ دوباره تضمین نمی‌شود.
      • ✅ `engine.jsx` / `admin_engine.dart` — ۱۰ فیلدِ انتزاعی (منحنیِ XP،
        آستانه‌هایِ ۰ تا ۱، چرخهٔ استریک) حالا می‌گویند عدد «چه چیزی را در
        بازی لمس می‌کند» و بازه‌اش از کجا می‌آید. متن‌ها از `adminOps.js` /
        `matchSettings.js` / `imageFingerprint.js` *خوانده* شدند (نسبتِ
        ۱٫۱۵ به رتبهٔ دوم، قاعدهٔ «review ≤ accept»، فرمولِ لول) — نه از
        حدسِ رابط‌ساز؛ یک توضیحِ قشنگِ غلط از نبودنش بدتر است.
      • 🐛 همین کار یک باگِ واقعیِ اندروید را لو داد: `_numField` پارامترِ
        `hint` داشت که روی `hintText` می‌نشست و در فیلدِ **پر** هرگز دیده
        نمی‌شد — یعنی هر «راهنماییِ» که تا امروز به آن پاس داده می‌شد، بی‌صدا
        دور ریخته می‌شد. `helperText` اضافه شد (`helperMaxLines: 3`).
      • ✅ `shop.jsx` / `admin_shop.dart` — ۹ توضیح (۱۰ ردیف، چون «درصد
        صرفه‌جویی» برای ماهانه و سالانه یکی است) + ۵ ردیفِ اندروید. همه از
        *رفتارِ واقعی* سرور/کلاینت نوشته شدند، نه از حدسِ رابط: «اگر `payload`
        خالی بماند کلاینت `slug` را مصرف می‌کند» (`shop_page.dart:1017`)،
        «قیمت منفی را سرور به ۰ می‌چسباند» (`adminShop.js:107`)، «از خطِ ششم در
        پلنِ ماهانه و از دهم در سالانه، مزیت نمایش داده نمی‌شود» (برشِ `slice(0,
        5|9)` در هر دو کلاینت) — آن آخری دقیقاً چیزی است که ادمین امروز *نمی‌داند*
        و فکر می‌کند پنل بی‌اعتناست.
      • ✅ بقیهٔ صفحات، یکی‌یکی و همه با **همان متن** در دو پنل: `settings` (۱۵
        توضیح)، `rewards` (۱۲)، `wallet` (۴)، `game-economy` (۱۰)، `wheel` (۵)،
        `photo-cards` (۷)، `admins` (۳)، `league` (۶)، `missions` (۵)،
        `battle-pass` (۶)، `notifications` (۳)، `points` (۳)، `game-rewards` (۴)،
        `users` (۳). شمارشِ **واقعِ** فایل‌ها (نه جمعِ ذهنی): ۱۱۳ `hint=` در ۱۷ صفحهٔ وب (پرشماری‌ها: `engine` ۱۷، `settings` ۱۵، `rewards` ۱۲، `shop` ۱۰) و ۶۲ `helperText: '…'` در اندروید؛ `login`/`support`/`metrics`/`dashboard`/
        `analytics` عمداً بدونِ توضیح‌اند چون ورودیِ قابل‌تنظیم ندارند.
      • روشِ کار مهم‌تر از فهرست است: هر توضیح از *رفتارِ خودِ سرور* نوشته شد و
        شمارهٔ سطرش در کامیت آمده (`min(22, √points/3.2)` برای امتیازِ کارت،
        `room = maxTiersPerDay − used` برای گذرِ نبرد، «۰ = بی‌سقف» در سقفِ
        امتیازِ مثبت در برابر «۰ = هیچ لولی بسته نمی‌شود» در سقفِ لولِ روز).
        جایی که متنِ پنل با کد نمی‌خواند، توضیح *رفتارِ واقعی* را نوشته، نه
        حرفِ عنوانِ کارت را.
      • 🐛 دو ناهمخوانی که *همین* کار لو داد (هر دو در کد درست‌اند، در متنِ پنل
        غلط — پس «رفعِ باگ» اینجا معنی‌اش اصلاحِ جمله است، نه اصلاحِ سرور):
        ۱. «کپی پاداش‌ها از فصل»: عنوانِ کارت می‌گوید «بدون الگو، فصل خالی
           ساخته می‌شود»، ولی `adminPass.js:100` با
           `COALESCE($1, آخرین فصلِ ساخته‌شده)` همیشه از آخرین فصل کپی می‌کند؛
           هیچ مسیری برای «واقعاً خالی» در پنل نیست. توضیح، رفتار را می‌گوید و
           راه‌حلِ عملی (پاک‌کردنِ ردیف‌ها پس از ساخت) را. اگر مالک «فصلِ خالی»
           را خواست، باید `template` صریح به معنی «هیچ» برسد — این **داده/طرح**
           است، نه باگِ اجرایی، و تا تأییدِ مالک دست به سرور نمی‌بریم.
        ۲. «برشِ فهرستِ مزایا»: قیمتِ پلن از پنل قابل تغییر است ولی کلاینت‌ها
           `slice(0, 5|9)` برش می‌زنند — سطرهایِ ششم به بعد هرگز دیده نمی‌شوند
           (در توضیحِ `shop` نوشته شد؛ برای «ویرایشِ متنِ پنل» نه برای عدد).
### فاز ۴ — به‌روزرسانیِ هوشمند — 🟨 (کدِ هر دو بند زده شد؛ تأییدِ آخر با CI)
- 🟨 `updateUrl` ← لینکِ کافه‌بازار از خودِ config + جمله‌های دیالوگ از `live_copy.update`
  • سرور لینک را می‌سازد: `app.updateUrl.android` (اگر ادمین پر کرده باشد همان) وگرنه
    `ops_limits.bazaarApiBase` + `app.bazaarPackage` (فیلدِ تازه، پیش‌فرض `ir.ghelgheli.shop`).
    چرا این سمتِ سرور: وب این رشته را در کد داشت (`|| 'https://ghelghelishop.ir'`) و اندروید
    هیچ fallbackی — یعنی در حالتِ پیش‌فرضِ مخزن، دکمهٔ وب کاربر را به *سایت* می‌برد نه صفحهٔ
    نصب، و دکمهٔ اندروید بی‌کار می‌ماند. «دامنهٔ پیش‌فرض» نباید در دو کلاینت دو نسخه داشته باشد.
  • ✅ فیلدِ «نامِ بستهٔ اپ در کافه‌بازار» در تنظیماتِ *هر دو* پنل اضافه شد (`settings.jsx`
    و `admin_settings.dart`: کنترلر، خواندنِ مقدار از GET، فرستادنش در PATCH). بی‌این فیلد،
    `app.bazaarPackage` در سرور بود ولی *هیچ‌جا* قابلِ عوض نیست — یعنی «تنظیماتِ فقط با
    curl». برچسب عمداً می‌گوید «وقتی لینکِ بالا خالی است» تا مدیر فکر نکند باید هر دو را پر کند.
  • گروهِ `update` (شش کلید: title/notice/body/action/reload/later) به `DEFAULT_COPY` و
    `COPY_CONTRACT` و جدولِ گروه‌هایِ *هر دو* پنل اضافه شد. ارقامِ نسخه از `{current}`/`{min}`
    می‌آیند، نه از رشته.
  • دیالوگِ اندروید و modalِ گیتِ وب یک کلیدِ مشترک می‌خوانند (`update.body`) و متنِ
    **واژه‌به‌واژه** یکی‌شان همان فول‌بک است؛ «دانلود / ورود» وب عمداً کلیدِ یک‌طرفه نشد.
- ✅ `configVersion` در لاگِ کرشِ **هر دو** کلاینت: وب `liveConfigVersion()` را در `context`
      می‌فرستد و اندروید `AppConfig.instance.configVersion` را. هیچ مایگریشن/رشتهٔ تازه‌ای
      لازم نشد (`analytics.reportCrash` همان `context` را ذخیره می‌کند) و `null` یعنی
      «config نرسیده» — نه ۰ که با نسخهٔ واقعی اشتباه گرفته شود.
- • سنجهٔ تازه در `live-copy-parity` (۵۰): «هیچ آدرسِ سایتی در کلاینت به‌عنوانِ fallbackِ
      لینکِ آپدیت سفت نشده» — همان بیماریِ عددِ سفت، در قالبِ دامنه.

### فاز ۵ — آزمونِ سرتاسری + عرضه — 🟨 (**«تستِ ۲۰ تغییر» رویِ پروداکشن اجرا و سبز شد**)؛ APK باقی است)
- ✅ «تستِ ۲۰ تغییر» — **اجرا شد، رویِ خودِ سرورِ تولیدی، بعد از دیپلوی**: `✅ 74 موفق، 0 ناموفق`
  (رویِ HEADِ `c6bc916`؛ دو اجرایِ اول — ۶۷✓/۲✗ و ۶۹✓/۴✗ — بعد از اصلاحِ خودِ ابزار
  در `3744817`/`49320fd` به ۷۴✓/۰✗ رسید). ۱۴ متن + ۶ عدد، تک‌به‌تک ذخیره شد؛ برای هر تغییر سنجیده شد
  که `configVersion` بالا رفته **و** همان رشته در `/api/config` **عمومیِ بی‌توکن**
  نشسته است — یعنی همان مسیری که مرورگرِ وب و `AppConfig` اندروید از آن می‌آیند،
  نه پاسخِ ادمین. در پایان هم بدنهٔ copy/rules مو‌به‌مو به ابتدا برگشت و ETag تازه شد.
  **سه باگِ واقعی از دلِ همین اجرا بیرون آمد** (هیچ‌کدام با ۱۰۰ تستِ لوکال دیده نمی‌شدند):
  ۱. کارتِ تاریخچهٔ هر دو پنل «(ادمین 3)» چاپ می‌کرد — یعنی شناسه در رابطی که قولش
     «زبانِ آدم» است؛ حالا `LEFT JOIN admin_users` نام را می‌آورد و fallback‌هایش
     («یک ادمینِ دیگر» / «تیم پشتیبانی») هیچ‌وقت عدد نیستند.
  ۲. `history/copy` بیست ردیف × اسنپ‌شاتِ کاملِ copy (~۸۰KB) برمی‌گرداند که هیچ
     مصرف‌کننده‌ای نداشت؛ `historyView()` ردیف را به پنج ستونِ لازم لاغر کرد.
  ۳. خودِ ابزار هم آن دو را اول قرمز می‌کرد: درخواستِ تاریخچه توکن نداشت و پاسخِ 401 را
     «تاریخچه خالی» گزارش می‌داد — اگر ابزار را روی دادهٔ واقعی اجرا نمی‌کردم، گاردِ
     محصول را با باگِ خودِ گارد اشتباه می‌گرفتم.
  `live_content_history` از ۰ به ۲۰+۲۰ ردیفِ واقعی رسید (سقفِ `HISTORY_KEEP = 20` برای
  هر کلید، که با «بیست تغییر» تصادفاً قشنگ جور شد).
  • ابزارش هست: `tools/e2e_livecopy20.py`، دو حالت — `--read-only` (بی‌رمز، بی‌نوشتن) و
    اجرایِ کامل با رمزِ ادمین. در حالتِ کامل ۲۰ تغییر **تک‌به‌تک** زده می‌شود و برای هر کدام
    سه چیز سنجیده می‌شود: بالا رفتنِ `configVersion`، دیده‌شدنِ همان رشته در `/api/config`
    **عمومیِ بدونِ توکن** (مسیرِ واقعیِ ETag/کش که کاربر از آن می‌آید، نه پاسخِ ادمین)، و در
    پایان مو‌به‌مو به ابتدا برمی‌گردد؛ اگر بازگردانی شکست exit ۲ و دستورِ بازگردانیِ دستی.
  • کالیبره رویِ پروداکشن: ✓ ۷ از ۷ — ۳۵ رشته در configِ زنده، `configVersion: 1`، ETag ست،
    و ابزار خودش گفت «گروهِ update هنوز در config نیست» چون آن commit deploy نشده. نه قرمزِ
    الکی، نه سبزِ کور.
  • نسخهٔ اولِ خودِ ابزار همان دامِ گاردهای کهنه را داشت: «بیش از ۴۰ رشته» می‌خواست و روی
    پروداکشن قرمز شد چون امروز ۳۵ رشته هست. سنجه به «پوششِ گروه‌ها» عوض شد — عددِ سقف‌دار
    یعنی تستی که عددِ منسوخ را قفل می‌کند.
  • ✅ اجرایِ **نوشتنی** رویِ پروداکشن انجام شد: «✅ 74 موفق / ❌ 0 ناموفق» — با ردیف‌هایِ
    واقعیِ `live_content_history` (`live_copy|20` و `live_rules|20`) و بازگردانیِ مو‌به‌مو
    در پایانِ تست؛ بعدش هم `--read-only` ✓۷ و صفرِ `[e2e]` هم در `/api/config` و هم در
    باندل‌هایِ ساخته‌شده. سه قرمزِ اولِ اجرا **ابزار** بود نه محصول (توکنِ فراموش‌شده در
    GET تاریخچه، سنجهٔ «برگشت‌پذیری» گذاشته‌شده در میانهٔ تست، و `req()` که سه‌تایی
    برمی‌گرداند) و هر سه با psql/curlِ دستی **پیش از** دست‌زدن به کد تشخیص داده شد.
- ✅ `npm test` + `flutter test` + `flutter analyze` صفر — از GitHub Actions، نه
      ورک‌اسپیس (اینجا عمداً فلوتر نصب نیست و نخواهد بود): پنج job روی `12af4d8` سبز، از
      جمله `analyze-and-test` که همان تغییرِ دارتِ ۳.۲ را کامپایل کرد.
- ✅ `tools/reset_for_launch.py` — ابزار *از قبل وجود داشت* و بندِ این فاز،
      «ساختنِ دوباره» نبود بلکه **اجراشدنی و آزموده‌شدن** آن بود؛ پیش از این به یک
      اسکریپتِ کمکیِ *بیرونِ مخزن* وابسته بود (با پاک‌سازیِ ورک‌اسپیس، ابزارِ لحظهٔ
      عرضه عملاً اجرا نمی‌شد — بدترین نوعِ وابستگی: چیزی که فقط رویِ ماشینِ یک نفر
      کار می‌کند و در روزِ انتشار کشف می‌شود). سه باگِ *واقعی* با اجرایِ ابزار رویِ
      **کپیِ دیتابیسِ پروداکشن** (`pg_dump` → `reset_probe*` + یک کاربرِ جعلی) پیدا و
      رفع شد؛ هیچ‌کدام رویِ پروداکشن اجرا نشد، فقط `--report`ِ فقط‌خواندنی:
      • `card_codes` در مایگریشن ۰۰۱ ساخته و در `080_stickers_and_card_codes_cleanup.sql`
        **حذف** شده بود، ولی `PURGE` و `REPORT` هنوز رو‌به‌رویش `DELETE`/`count` داشتند →
        اجرایِ `--yes` رویِ همین schema با «relation does not exist» می‌سوخت. حالا
        `prune_missing()` فهرستِ جدول‌ها را از دیتابیس می‌پرسد، خط‌هایِ بی‌جدول را برمی‌دارد
        و *نامشان را گزارش می‌دهد* (سکوت در ابزارِ پاکسازی = دروغ).
      • `psql` بیِ `ON_ERROR_STOP=1` اجرا می‌شد: خطایِ وسطِ تراکنش با exit ۰ برمی‌گشت و
        «پاکسازیِ نصفه» سبز رد می‌شد. اضافه شد `SET LOCAL`/ترتیبِ درست، و **بازبینیِ
        شمارش** پس از حذف: اگر جدولی صفر نشده باشد، exit غیرصفر با نامِ همان جدول.
      • `/tmp/keep.txt`ِ مشترک بینِ دو اجرایِ هم‌زمان (یکی رویِ پروداکشن، یکی رویِ کپیِ
        آزمون) — فایلِ موقت حالا با نامِ دیتابیس قفل می‌شود، چون این ابزار **فایل حذف
        می‌کند** و فهرستِ اشتباه = حذفِ فایلِ درست.
      • بکاپ اجباری شد: `pg_dump | gzip` پیش از هر حذف + سنجشِ **حجمِ فایل**، نه فقط
        exit code (`… | gzip` با لولهٔ شکسته هم ممکن است صفر برگرداند)؛ گزینهٔ
        `--no-backup` فقط برایِ آزمون رویِ کپی. `--db`/`--uploads` هم برای همین اضافه شد:
        بیِ آن‌ها آزمودنِ این ابزار یعنی *رویِ پروداکشن* زدنش.
      • نتیجه رویِ کپی: `users=1` (فقط حسابِ اصلیِ مدیر، بقیه صفر) در حالی که
        `app_settings=26`، `card_box_odds=5`، `chat_stickers=10`، `live_content_history=40`
        دست‌نخورده ماندند؛ `75` فایلِ یتیم از `9.9M` حذف شد و `56` فایلِ ارجاعی سرِ جایش
        ماند. اجرایِ دوم رویِ همان کپی **بی‌خطا** بود (idempotent) — و گزارشِ `--report`
        رویِ پروداکشن هم فقط‌خواندنی است: ۱۳۱ فایل / ۶۱M، بدونِ هیچ نوشتنی.
      • نگهبانِ استاتیکِ تازه: `backend/scripts/testResetGuard.js` (۱۸ سنجه، ثبت‌شده در
        `npm test`). همین گارد در نخستین اجرا شش جدولِ «بدون‌دسته» گرفت (`card_box_odds`،
        `mission_definitions`، `reward_groups`، `chat_stickers`، `league_perk_tiers`،
        `live_content_history`) — یعنی دقیقاً همان الگویِ تاریخیِ «جدولِ تازه، پاک‌سازِ
        کهنه» (سه بار تکرار شده: `user_coin_quota`، `card_box_purchases`، `league_perk_awards`).
        پاسخِ درست «افزودنشان به DELETE» نبود؛ «ثبتِ صریحِ دست‌نخورده‌بودنشان» بود.

- ⬜ buildِ **یک** APK (آپدیتِ ساختاری) — عمداً فقط با درخواستِ صریحِ مالک ساخته می‌شود.
      کدِ دارتِ ۳.۲ و فیلدِ `bazaarPackage` در مخزن و سبزِ CI است، ولی در بیلدِ
      نصب‌شدهٔ گوشی هنوز «منوی بی‌گروه» دیده می‌شود: ظاهری است، نه رفتاری.

## ۴) خطاها و شکاف‌ها (وضعیت)

| # | مورد | فاز | وضعیت |
|---|---|---|---|
| ۱ | شمارندهٔ جوایزِ اندروید /۳۰ | ۰ | ✅ `1bc7215` |
| ۲ | هدیهٔ عضویت در پنل اندروید | ۰ | ✅ رد شد (مثبتِ کاذب) |
| ۳ | دو نسلِ کد اتاق | ۰/۱ | ✅ یکدست + `roomCodeLength` زنده؛ خواندنِ طول در فاز ۲ |
| ۴ | باقی‌ماندهٔ تست در DB عرضه | ۰ | ✅ پاک شد |
| ۵ | README کهنه | ۰ | ✅ |
| ۶ | «۱» هاردکدِ قانونِ دعوت | ۰ | ✅ هر دو کلاینت: `referral.dailySpinRule` در `Referral.jsx` + `wheel_page.dart:1192` + `referral_page.dart:462` (بررسیِ `grep` در همین نشست؛ سطرِ «⬜ اندروید» کهنه بود) |
| ۷ | «۲۴ ساعت» SLA بدون ثابت | ۱/۲ | ✅ `reviewSlaHours` + متنِ هر دو کلاینت (`PhotoCardBox.jsx` و `photo_card_box.dart` هر دو از یک کلید می‌خوانند) |
| ۸ | ۷ fetch اندروید / ۴ fetch وب | ۲ | ✅ هر دو از یک منبع (AppConfig در اندروید، استورِ config در وب)؛ گاردِ `testLiveWiring` سفیدفهرستِ ۷ نقطه را قفل کرده تا نقطهٔ تازه‌ای مستقل نزند |
| ۹ | مستنداتِ کهنه | ۰ | ✅ (این نقشه‌راه هم از گفت‌وگو به مخزن منتقل شد) |
| ۱۰ | APK ۹ کامیت عقب | ۵ | ⬜ |
| ۱۱ | `reward_tiers`/`reward_tier_cards` خالی | ۵ | ⬜ (داده، نه کد) |
| ۱۲ | چرخاندنِ رمزهای لوشده | ۰ | ✅ مالک انجام داد: PATِ قدیمیِ GitHub ۴۰۱ می‌دهد و تازه‌ای ۲۰۰؛ رمزِ rootِ سرور عوض شد (واحد‌های `invoicle-*` پس از آن active) |
| ۱۳ | منوی ۲۳ قلمیِ بدون گروه | ۳ | ✅ (۸ گروه، هر دو پنل، گاردِ ۴۰ سنجه‌ای) |
| ۱۴ | فول‌بک‌های عددیِ پراکندهٔ کلاینت | ۲ | ✅ هفتِ باقی‌مانده همه «مستند»اند: منطقِ سرور (ساعتِ ریستِ گردونه، `DECK_SIZE`)، متنِ دموی `games.jsx`، و فول‌بک‌هایی که *مقادیرِ امروزِ محصول*‌اند — در `games_page.dart` خواندن از `AppConfig.instance.apply(cfg)` سطرِ ۱۹۳ هست و پنج خواندنِ `liveText`/`liveRule` کنارش (این بندِ جدول کهنه بود؛ با grepِ همین نشست تصحیح شد) |
| ۱۵ | همسانیِ «راهنمایِ فیلدها» | ۳.۴ | ✅ گاردِ `userweb/tool/admin-hint-parity.mjs` (۱۴۶ سنجه، از جمله دو سنجهٔ «ساختار» که CIِ `4484cb4` را قرمز کرد) + پلِ `backend/scripts/testAdminHintParity.js` در `npm test` — نخستین اجرایِ خودش دو صفحه را قرمز کرد (`admins` و `points` فقط در وب توضیح گرفته بودند) و بجایِ سفیدکردنِ گارد، خودِ اندروید وصل شد. سه استثنا با *دلیلِ نوشته‌شده* باقی است (سقفِ ۳) و ۳۳ «بدهیِ شمرده» گزارش می‌شود، نه خطا: برچسب‌هایی که در دو پنل یکی نیستند
- ✅ **گاردِ راهنماها** (`userweb/tool/admin-hint-parity.mjs`): هیچ `helperText` ثابتی در اندروید نمی‌تواند «بی‌ریشه» باشد (عینِ یک `hint` وب یا زیررشتهٔ یکی)، صفحه‌ای که وبش توضیح دارد نمی‌تواند در اندروید *هیچ* توضیحی نداشته باشد، راهنماها نمی‌توانند به `liveContent.js` راه یابند، و سقفِ استثنائات/`helperText`هایِ داینامیک قفل است. «تعدادِ بدهی» چاپ می‌شود تا رشدِ بی‌صدا پیداتا باشد.

## ۵) پیوست A — `app_settings`: کی دستِ چه کسی است

`client_config` · `shop_plus_plans` · `game_economy_settings` · `game_reward_settings` ·
`mission_config` · `pass_config` · `level_settings` · `photo_match_settings` · `streak_settings` ·
`wallet_settings` · `signup_gift` · `chat_*` · `league_*` · `card_box_price` · `sms_config` ·
`ops_limits` · **`live_copy`** · **`live_rules`** · `config_version`

## ۶) پیوست B — چه چیزی هرگز آپدیت می‌خواهد

بازی/صفحهٔ تازه · تغییرِ ساختار UI · ارتقای SDK · داراییِ باینریِ تازه
(آواتار و استیکر استثنا‌اند: تصویر از سرور سرو می‌شود).

## ۷) ریسک‌ها و سپرها

خرابیِ محصولِ زنده → هر فاز مستقل + rollback خودکارِ `deploy.sh` + deploy کم‌ترافیک
کلاینتِ قدیمی → کلیدها افزودنی‌اند و نادیده گرفته می‌شوند
عددِ خطرناک از پنل → بازهٔ امن + پیش‌نمایش + واپس‌گیری + audit
شکستنِ RTL/قالب → تستِ آینگی + `smoke.mjs`
گسستِ دوبارهٔ دو پنل → `testAdminParity.js` + `testLiveWiring.js` + قراردادِ کتبی
ورک‌اسپیس → تست/آنالیزِ سنگین در ورک‌اسپیس **نمی‌گیریم**; از GitHub Actions

## ۸) تعریفِ پایان

۱. تستِ ۲۰ تغییر · ۲. پاریتیِ ۱۰۰٪ دو پنل · ۳. نگهبانِ رقمِ فارسی: صفر یافتنی خارجِ سفیدفهرست ·
۴. ادمینِ غیرفنی در ۱۰ دقیقه قیمت+متن را عوض کند و پیش‌نمایش ببیند ·
۵. `flutter analyze` صفر + `npm test` سبز + CI سبز (✅) · ۶. خروجیِ تمیزِ `reset_for_launch.py`
(✅ رویِ کپیِ دیتابیسِ پروداکشن) · ۷. APKِ تازه (⬜ با درخواستِ صریحِ مالک)

## ۹) برآورد

حذف‌شد: «روز» و «هفته» برای این پروژه دروغ گفت (فاز ۳ که «۵–۷ روز» بود در
یک نشستِ کاری با یک deploy تمام شد، و فاز ۵ که «۱–۲ روز» بود به یک اجرایِ
ابزار رویِ پروداکشن نیاز داشت — نه به زمان، به *اجرا*). معیارِ تازه، فهرستِ
باقی‌مانده است، همان‌طور که در «تعریفِ پایان» بالا نوشته شده: ⬜ها یکی‌یکی با
کامیت/اجرایِ واقعی پاک می‌شوند، نه با تقویم. آنچه امروز باز است: APK (منتظرِ
درخواستِ صریح)، چرخاندنِ رمزها (کارِ مالک)، دادهٔ `reward_tiers` (داده، نه
کد)، اجرایِ `--yes`ِ reset در شبِ عرضه، و خواندنِ پنل توسطِ «ادمینِ تازه‌کار».
