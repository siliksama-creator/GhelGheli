# گزارش — ماموریت‌های اختصاصیِ چندتایی (چند کمپین هم‌زمان)

> خواستهٔ مالک: «باید یه کاری کنی که اگه ادمین خواست چند تا ماموریت اختصاصی بتونه قرار بده.»

قبل از این تغییر، ادمین فقط **یک** کارتِ ماموریت اختصاصی می‌توانست بگذارد.
حالا فهرستی از کارت‌ها می‌گذارد (پیش‌فرض ۵ تا) که همه بالای «ماموریت‌های
امروز» نشان داده می‌شوند — بدونِ آپدیتِ اپ.

## ۱) ادمین چه کاری می‌کند

| کار | جای پنل |
|---|---|
| افزودن کارتِ تازه | دکمهٔ «افزودن ماموریت» |
| جابه‌جاییِ ترتیب | فلشِ بالا/پایینِ هر کارت (ترتیب = ترتیبِ نمایش به کاربر) |
| روشن/خاموشِ هر کارت | کلیدِ «فعال باشد» داخلِ هر کارت |
| حذفِ کارت | دکمهٔ «حذف» (امتیازهای گرفته‌شده پاک نمی‌شود) |
| «دورهٔ تازه» | همان کارت با شناسهٔ نو: همه — حتی کسانی که گرفته‌اند — دوباره می‌توانند امتیاز بگیرند |
| شمارِ دریافت‌کنندگان | نشانِ «N دریافت» روی هر کارت (آمارِ زنده از دفترِ پیشرفت) |

ذخیره **اتمیک** است: کلِ فهرست با یک `PUT` می‌نشیند؛ یا همه یا هیچ‌کدام.

## ۲) قاعده‌ای که عوض نشد

- هر کاربر برای **هر ماموریت** یک‌بار امتیاز می‌گیرد؛ بعد آن کارت برای او
  پنهان می‌شود (خواستهٔ صریح مالک).
- امتیاز با منبعِ `mission` در دفترِ امتیاز می‌نشیند: «ماموریت اختصاصی: …».
- لینک فقط `http(s)`؛ متنِ خالیِ لینک ⇒ «اینجا کلیک کنید»؛ رنگ آبی/سبز.

## ۳) چیزهایی که عمداً نشکستند

- **اپِ نصب‌شده روی گوشیِ کاربران**: کلیدِ `custom` (تک‌ماموریتی) و مسیرِ
  `POST /api/missions/custom/claim` سرِ جایشان هستند، پس APKهای امروز یا
  کارتشان را از دست نمی‌دهند یا دکمهٔ دریافتشان نمی‌شکند. آن‌ها اولین
  ماموریتِ فعال را می‌بینند.
- **دادهٔ روی سرورِ زنده**: شکلِ قدیمیِ `app_settings.custom_mission` خوانده
  می‌شود و مثلِ فهرستِ تک‌عضوی دیده می‌شود — بدونِ مایگریشن.
- **ویرایشِ متن = همان دوره**: اصلاحِ غلطِ تایپی به همه امتیازِ تازه نمی‌دهد
  (شناسه دست‌نخورده می‌ماند). برای کمپینِ دوباره، «دورهٔ تازه» هست.

## ۴) قراردادها

```
app_settings.custom_mission = {
  items: [ { id, enabled, title, body, points,
             link: { url, text, color }, updatedAt, updatedBy } ],
  updatedAt, updatedBy
}

GET    /api/admin/custom-mission              → { missions, max, colors, preview, stats, … }
PUT    /api/admin/custom-mission              ← { items: [...] }        (شکلِ قدیمیِ تک‌ماموریتی هم پذیرفته می‌شود)
POST   /api/admin/custom-mission/:id/reset    → دورهٔ تازه با شناسهٔ نو
POST   /api/missions/custom/claim             → اولین ماموریتِ نگرفتهٔ کاربر  (کلاینتِ قدیمی)
POST   /api/missions/custom/:id/claim         → همان ماموریتِ خواسته‌شده    (کلاینتِ تازه)
GET    /api/missions , /api/growth/overview   → { custom, customMissions: [...] }
```

`customMissions` فقط کارت‌هایی را می‌دهد که **همین کاربر نگرفته** است.

## ۵) گاردها

- `backend/scripts/testMultiCustomMission.js` — ۵۳ بررسی: سازگاریِ دادهٔ
  قدیمی، ترتیب، شناسه‌ها، سقف، اعتبارسنجی، وضعیتِ هر کاربر، دریافتِ
  ماموریتِ مشخص، دورهٔ تازه، آمار، و سیم‌کشیِ **هر سه** کلاینت
  (وب `GrowthHub.jsx`، اندروید `growth_panel.dart`، پنل `custom-mission.jsx`).
- `testValidation.js` — همین گارد نسخهٔ اولِ مسیرِ `:id/reset` را گرفت چون
  `validateUuid` نداشت؛ حالا دارد.
- `reward-parity` (وب ↔ اندروید) — منبعِ «جشنِ دریافت» برای ماموریت‌های
  اختصاصی همان `custom` می‌ماند.

## ۶) تله‌ای که خوردیم و درسش

مسیرِ `:id` بدونِ `validateUuid` در `npm test` قرمز شد. اولین نسخه‌ای که
نوشتم، گاردِ `testValidation.js` (بندِ «هر مسیرِ :id اعتبارسنجی دارد») آن را
گرفت — یعنی گاردها همان‌جا که باید کار می‌کنند. اگر مسیرِ تازهٔ `:id` اضافه
می‌کنی، گاردِ شناسه را از یاد نبر.

## ۷) وضعیتِ نهایی (۲۹ شهریور)

| کامیت | کار |
|---|---|
| `261bb67` | سرویس/مسیرها/پنل/وب/اندروید — چند ماموریتی + گاردِ ۵۳ بررسی |
| `274e430` | `super.key` روی `_CustomMissionCard` (خطای تحلیلِ Flutter Check) |
| `ba57252` | تستِ سرتاسری روی سرورِ واقعی + اجرای دستی با دیتابیسِ دوری + step در CI |
| `d1a1a95` | رفعِ سرریزِ کامنت که `const BASE` را داخلِ کامنت انداخته بود |

- **CI**: «Backend and Admin CI» روی `d1a1a95` سبز (۴/۴ جاب، شاملِ
  «E2E multi custom mission»)، «Flutter Check» روی `274e430` سبز.
- **سرور**: `d1a1a95`، هلسِ هر سه دامنه ۲۰۰.
- **تأییدِ زنده**: پنلِ ادمین سه کارت می‌سازد/ویرایش می‌کند (اسکرین‌شات)،
  کاربرِ تازه دو کارتِ فعال را می‌بیند، امتیازِ هر کارت جداگانه و یک‌بار
  واریز می‌شود، و ردیفِ «ماموریت اختصاصی: …» در دفترِ امتیاز می‌نشیند.

---

## ۸) دورِ دوم — رفعِ باگِ «فهرست دوباره برمی‌گردد روی یکی» (۲۹ شهریور)

مالک: «باگِ اساسی داره؛ حتی روی ۲ ماموریت قرار نمی‌گیره و لیست دوباره برمی‌گرده
روی یکی. باید تا ۱۰ تا ماموریت هم بتونم ادد/حذف/ویرایش کنم و همون ماموریت رو
دوباره به کاربرا بفرستم.»

### ریشهٔ باگ (بازتولیدِ زنده، نه حدس)

سرور **چندپروسه** است: گرهِ بازی روی ۴۰۰۰ + چهار گرهِ HTTP روی ۴۰۰۱–۴۰۰۴ و
nginx بینِ این پنج پورت round-robin می‌کند (`/etc/nginx/snippets/ghelgheli-upstream.conf`).
ولی `opsConfig` مقدارها را در `Map` **درونِ همان پروسه** کش می‌کرد و `set()`
فقط کشِ همان پروسه را تازه می‌کرد. کامنتِ قدیمی هم می‌گفت «سرور تک‌پروسه است»
— همان جمله‌ای که باعث شده بود کسی سراغِ هم‌گامی نرود.

بازتولید روی دامنهٔ زنده (قبل از رفع):

```
PUT دو ماموریت → «۲ ماموریت ذخیره شد»
۱۲ بار GET      → [1,1,1,1,2,1,1,1,1,2,1,1]
```

یعنی چهار پروسه از پنج، فهرستِ کهنه را می‌دادند: ادمین کارتِ دوم را می‌ساخت و
«ناپدید» می‌شد، و کاربر گاهی ماموریتی را می‌دید که پاک شده بود. همین کلاسِ
باگ برای متن‌های زنده، اعدادِ گذر نبرد و سقف‌های عملیاتی هم وجود داشت.

### رفع (دو لایه)

1. **Redis Pub/Sub** — هر `set()` روی کانالِ `ghelgheli:ops-invalidate` پیام
   می‌فرستد و هر پروسه با شنیدنش همان کلید را از دیتابیس تازه می‌کند
   (همان الگوی `rateLimitStore` و آداپتورِ سوکت).
2. **گاردِ TTL (۴ ثانیه)** — تورِ ایمنی بدونِ زیرساخت: مقدارِ کهنه سرو می‌شود
   (مسیرِ داغ بلاک نمی‌شود) ولی تازه‌سازیِ پس‌زمینه می‌خورد. اگر Redis بخوابد
   یا پیام گم شود، واگرایی حداکثر چند ثانیه خودش جمع می‌شود.

### آنچه هم‌زمان اعمال شد (خواستهٔ مالک)

* **سقف از ۵ به ۱۰** (`MAX_ITEMS`)، یک عدد در سرور؛ پنل آن را از پاسخِ API
  می‌خواند و با رقمِ فارسی نشان می‌دهد.
* **«ارسال دوباره به همه»** جای «دورهٔ تازه» — همان ماموریت با شناسهٔ تازه،
  پس همه (از جمله کسانی که امتیاز گرفته‌اند) دوباره می‌بینند.
* **ویرایش، کارت را برنمی‌گرداند**: شناسه دست‌نخورده می‌ماند؛ این نکته در
  پنل هم صریح نوشته شد.
* **گاردِ «فهرستِ کهنه»**: پنل مُهرِ زمانیِ فهرست را همراهِ ذخیره می‌فرستد؛
  اگر تبِ دیگری در این فاصله ذخیره کرده باشد، سرور **۴۰۹** می‌دهد و کارِ
  ادمینِ دیگر پاک نمی‌شود (ویرایش‌های همان صفحه هم از دست نمی‌رود).

### گاردهای تازه

* `backend/scripts/testOpsCoherence.js` (۲۲ بررسی) — با دو **نمونهٔ مستقل** از
  ماژول (هر `require` تازه = یک کشِ جدا، مثلِ یک پروسهٔ جدا): هم انتشار/اشتراک
  و هم TTL سنجیده می‌شود. هر دو لایه **fail-test** شده‌اند (با خاموش‌کردنِ
  `invalidate` و با `TTL` بی‌نهایت قرمز می‌شود).
* `testMultiCustomMission.js` (۶۴ بررسی) + «سقفِ ۱۰» + «۴۰۹» + «۱۰ تا قبول،
  ۱۱ تا رد» در `e2eMultiMission.mjs`.

### وضعیتِ نهایی

| کامیت | کار |
|---|---|
| `efbbaf0` | رفعِ باگِ چندپروسه‌ای + سقفِ ۱۰ + «ارسال دوباره به همه» + گاردِ ۴۰۹ |
| `c477324` | هم‌راست‌کردنِ E2E با قراردادِ تازه (۱۰ و ۴۰۹) |
| `fff1142` | رقمِ فارسیِ سقف در پنل |

* **CI**: «Backend and Admin CI» روی `c477324` سبز (شاملِ E2E).
* **تأییدِ زنده (اسکریپتِ `live-missions-e2e.mjs`، ۱۶ بررسی)**: ۱۲ خواندنِ
  پیاپی همه «۲» داد؛ کاربرِ وب هر دو کارت را دید؛ بعد از دریافتِ یکی، فقط
  همان از فهرستش رفت و دیگری ماند؛ کاربرِ دیگری که نگرفته بود همچنان دیدش؛
  ویرایش کارت را برنگرداند؛ «ارسال دوباره به همه» برگرداند؛ ذخیره با مُهرِ
  قدیمی ۴۰۹ گرفت و فهرست دست‌نخورده ماند. در پایان، فهرستِ مالک به همان یک
  ماموریتِ اصلی برگشت.
* **پنل**: صفحهٔ «ماموریت اختصاصی» زنده بررسی شد (سقفِ ۱۰، دکمهٔ «ارسال
  دوباره به همه»، توضیحِ ویرایش-نمی‌فرستد، پیش‌نمایشِ زنده).
