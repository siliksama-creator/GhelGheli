# وب: ریسپانسیو، سرعت، و یکپارچگی موبایل/دسکتاپ — TODO و سند وضعیت

> سندِ کار برای ایجنت‌های بعدی. قبل از دست‌زدن به چیدمان/بیلد/nginx وب این را
> بخوانید. این فهرست **وضعیت واقعی** را نشان می‌دهد: هر چیزی که در مخزن یا روی
> سرور زنده سنجیده و تأیید شد تیک خورده (`[x]`)؛ کارِ ناتمام `[ ]` و کارِ
> نیمه‌انجام `[~]` است. تاریخ‌گذاری‌ها به تقویم شمسی است.

دو پرتال وب داریم (هر دو Vite + React، روی nginx):
- **پنل وب ادمین** — `admin/`، دامنه `admin.ghelghelishop.ir` (تنها راه مدیریت؛
  پنل ادمین از اپ موبایل حذف شده، بنگرید به `ADMIN_PANEL_MOBILE_RETIREMENT.md`).
- **وب کاربر** — `userweb/`، دامنه‌های `user.ghelghelishop.ir` و
  `register.ghelghelishop.ir` (آینهٔ وبِ اپ موبایل).

قیدهای پابرجا:
- **وب کاربر باید آینهٔ اندروید بماند.** هر اصلاحِ دسکتاپ فقط داخل
  `@media (min-width: …)` می‌نشیند؛ قواعد پایه (موبایل) دست نمی‌خورد. تست
  `userweb/tool/desktop-layout.mjs` همین را می‌بندد.
- بک‌اند، مسیرهای پول/Invoicle و 2FA ادمین دست‌نخورده.

---

## A. وضعیت فعلی (تأییدشده با خواندن کد و سنجش سرور زنده)

### A1. ریسپانسیو — پایه‌ها از قبل ساخته شده
- [x] **A1.1** ادمین: پوستهٔ ریسپانسیو با سایدبار دائمی روی دسکتاپ و **کشوی
  همبرگری + scrim** روی موبایل (`admin/src/components/app-shell.jsx`).
- [x] **A1.2** ادمین: باگِ کشوی موبایل در **RTL** شناسایی و حل شده
  (`html[dir='rtl'] .sidebar.open { transform: translateX(0) }` در `styles.css`).
- [x] **A1.3** ادمین: گریدهای کارت در سه نقطه می‌شکنند (1100 / 960 / 640px) و
  کارت‌های مبلغ روی تبلت ۲ستونه می‌شوند تا ارقام تومانی به هم نچسبند.
- [x] **A1.4** ادمین: جداولِ پهن داخل `.table-wrap { overflow:auto }` با سرستون
  چسبان (`position:sticky`) هستند — روی موبایل افقی اسکرول می‌شوند، نه اینکه
  صفحه را بشکنند.
- [x] **A1.5** ادمین: فرم‌های پرستونی روی موبایل تک‌ستونه می‌شوند
  (`@media(max-width:620px) .ptForm{grid-template-columns:1fr}` و مانند آن).
- [x] **A1.6** هر دو: `<meta viewport>` درست است (کاربر `viewport-fit=cover`
  هم دارد برای ناچ گوشی).
- [x] **A1.7** کاربرِ وب: چیدمان دسکتاپ بالغ است — ناوبری به «پیل شناور» تبدیل
  می‌شود، خانه/بازی‌ها چندستونه، و **داخلِ بازی دوستونه** با ریلِ کناری
  (اسکوربورد/پات/خطا) در `min-width:1100px`؛ نقاط شکستِ ارتفاع برای لپ‌تاپِ
  کوتاه (max-height 900/820) هم هست.
- [x] **A1.8** کاربرِ وب: تستِ ایستای `test:desktop-layout` چیدمان دسکتاپ و
  آینه‌ماندنِ موبایل را در CI می‌بندد.

### A2. سرعت — بخش بزرگی از کار از قبل انجام شده
- [x] **A2.1** هر دو پنل صفحات را با `React.lazy + Suspense` تکه‌تکه بار
  می‌کنند؛ صفحهٔ ورود و داشبورد سبک می‌آیند و بقیه صفحات هنگام باز شدن دانلود
  می‌شوند (ادمین `main.jsx` خط ~۲۹–۵۱؛ کاربر `main.jsx` خط ~۴۰–۵۳).
- [x] **A2.2** کتابخانه‌های سنگین ML (تشخیص چهره/کارت، onnxruntime) فقط هنگام
  نیاز **dynamic-import** می‌شوند و خودِ مدل‌ها (`/ml/*.onnx`, `.wasm`,
  `ort.min.js` ≈ ۶۵MB) در باندل اولیه نیستند.
- [x] **A2.3** کش استاتیک nginx: فایل‌های hashدارِ Vite یک سال، فونت/تصویر/صدا
  ۳۰ روز؛ و `index.html` و سرویس‌ورکر عمداً `no-cache` تا نسخهٔ تازه همیشه دیده
  شود (سنجش سرور: هدرها تأیید شد).
- [x] **A2.4** **gzip** روی سرور فعال است (هدر `Content-Encoding: gzip` تأیید شد).
- [x] **A2.5** کاربرِ وب سرویس‌ورکرِ کشِ تصویر دارد
  (`userweb/public/image-cache-sw.js` + `lib/imageCache.js`).
- [x] **A2.6** فونت‌ها Vazirmatn woff2 هستند (کاربر ~۳۶KB/وزن، ادمین ~۵۲KB).

---

## B. سرعت/تحویل روی سرور

- [x] **B1. (بالا) HTTP/2.** — **انجام شد (۱۴۰۵/۰۹/۱۶):** هر سه بلوک ۴۴۳ به
  `listen 443 ssl http2;` تغییر کرد (سبک قدیم، سازگار با nginx 1.24 سرور).
  تأیید زنده: هر سه دامنه حالا `HTTP/2 200` پاسخ می‌دهند. نتیجه: مالتی‌پلکسِ
  ده‌ها فایل JS/CSS/woff2 روی یک اتصال، بارگذاریِ اولِ سریع‌تر مخصوصاً روی
  موبایلِ با RTT بالا.
- [x] **B2. (بالا) کشِ بلندمدت فایل‌های بزرگ `/ml/`.** — **انجام شد:** سنجش
  نشان داد `sface.onnx` (۳۷MB)، `card_embed…onnx` (۱۷MB)، `ort-wasm….wasm`
  (۱۱MB) و `ort.min.js` از catch-all با `no-cache` رد می‌شدند و هر بار باز شدنِ
  صفحهٔ کارت revalidate/دانلود می‌کردند. بلوک `location ^~ /ml/` با
  `Cache-Control: public, max-age=2592000` (۳۰ روز) به هر دو سایت اضافه و تأیید
  زنده شد.
- [ ] **B3. (متوسط) Brotli برای محتوای متنی.** gzip هست ولی br برای JS/CSS/SVG
  ~۱۵–۲۰٪ کوچک‌تر فشرده می‌کند. نیاز به نصب ماژول `brotli` روی nginx دارد
  (روی اوبونتو پیش‌فرض نیست) و چون نصب پکیج سیستمی است، با احتیاط و در پنجرهٔ
  نگه‌داری انجام شود. فقط روی `text/*`, `application/javascript`,
  `application/wasm`, `image/svg+xml`؛ روی onnx/تصویر فایده ندارد.
- [x] **B4. (متوسط) پیش‌اتصال (preconnect).** — **انجام شد:** کاربرِ وب از قبل
  `preconnect` به `api.ghelghelishop.ir` داشت؛ به پنل **ادمین** هم
  `preconnect` + `dns-prefetch` به همان دامنه اضافه شد (RTT اول روی موبایل کم
  می‌شود).
- [x] **B5. (پایین) preload فونتِ وزن اصلی.** — **انجام شد:** کاربرِ وب از قبل
  دو وزنِ روی‌صفحه را preload می‌کرد؛ به پنل ادمین هم preload دو وزن
  (Regular برای بدنه، Bold برای تیتر/دکمه) اضافه شد تا FOUT/reflow نگیرد.
  (۱۰۳ Early Hints هنوز فعال نیست — موکول به آینده.)
- [x] **B6. (پایین) `immutable` روی کش `/assets/`.** — **انجام شد:** فایل‌های
  hashدار حالا `Cache-Control: public, max-age=31536000, immutable` می‌دهند
  (تأیید زنده) تا مرورگر حتی revalidate شرطی هم نکند.

> نکتهٔ معماری برای B2: مدل‌های ML بین ادمین و کاربر **تکراری**اند (۶۵MB در هر
> دو `public/`). چون روی دو دامنهٔ مجزا سرو می‌شوند، کش مرورگر مشترک نیست. اگر
> روزی خواستید، می‌توان `/ml/` را فقط از یک دامنهٔ static مشترک سرو کرد تا یک
> بار دانلود شود — کارِ جداگانه، فوری نیست.

---

## C. ریسپانسیوِ ادمین روی موبایل (تجربهٔ لمسی مدیر)

پوسته و گریدها آماده‌اند؛ این‌ها ریزه‌کاری‌های تجربهٔ لمسی‌اند که باید روی نمای
واقعیِ گوشی (≈۳۹۰px) بازبینی شوند.

- [ ] **C1. (بالا) صفِ تأیید کارت (مهم‌ترین صفحهٔ مدیریتیِ لمسی).** اطمینان از
  اینکه روی موبایل: تصویر کارت کامل دیده می‌شود، دکمه‌های «تأیید/رد» تمام‌عرض و
  زیر دست‌اند (`.adminCardActions .btn{width:100%}` هست)، و مقایسهٔ چهره/کارت
  (`adminCardSides`) بدون برش افقی کار می‌کند. **روی نمای واقعی گوشی یک بار
  end-to-end تست شود.**
- [x] **C2. (بالا) هدف‌های لمسی (tap target ≥ ۴۴px).** — **انجام شد:** در
  `@media(max-width:640px)` پنل ادمین، `.btn-icon` حداقل ۴۴×۴۴ شد (دکمه‌های
  کوچکِ عملیات جدول ۴۰×۴۰). فقط داخل media query موبایل؛ دسکتاپ دست‌نخورده.
- [ ] **C3. (متوسط) جدول‌های دیتا روی موبایل.** `.table-wrap` افقی اسکرول
  می‌شود (امن) ولی تجربهٔ خوب نیست؛ برای پرکاربردترین جداول (کاربران، برداشت‌ها،
  جوایز) حالت کارتی در عرض موبایل یا پنهان‌کردن ستون‌های کم‌اهمیت زیر
  `max-width:640px` در نظر گرفته شود تا اسکرول افقی کمینه شود.
- [~] **C4. (متوسط) فرم‌های طولانی (اقتصاد بازی، جوایز، pass).** — **بخشی
  انجام شد:** `.tier-edit` که `min-width:300px` داشت و روی گوشی باریک (~۳۶۰px)
  سرریز می‌کرد، در موبایل `min-width:0; flex-wrap:wrap` شد. بازبینیِ کاملِ بقیهٔ
  فرم‌ها روی نمای واقعی گوشی باقی است.
- [ ] **C5. (پایین) دکمهٔ بستن (X) داخلِ سایدبار** برای دسترسی‌پذیری (الان فقط
  scrim و انتخابِ آیتم کشو را می‌بندد).
- [ ] **C6. (پایین) state بارگذاری/خالیِ مخصوص موبایل** برای صفحات سنگین.

> معیار پذیرش این بخش: مدیر روی **مرورگر گوشی** بتواند بدون اسکرولِ افقیِ صفحه و
> بدون دکمه‌های ریز، کار روزانه (تأیید کارت، پاسخ پشتیبانی، دید آمار) را انجام
> دهد.

---

## D. یکپارچگیِ موبایل/دسکتاپ (هر دو پرتال)

- [x] **D1.** ادمین و کاربر هر دو روی همان زبانِ طراحی (توکن‌های تم، فونت
  Vazirmatn، گوشه‌های گرد، حرکت) بنا شده‌اند و ادمین عمداً از الگوی «ریل/کشو»ی
  اپ موبایل پیروی می‌کند تا خانوادهٔ محصول یکدست باشد.
- [ ] **D2. (متوسط) یکسان‌سازیِ نقاط شکست (breakpoint).** کاربر عمدتاً
  `1100/900/640/520` دارد، ادمین `1100/960/640`. مرز تبلت (≈۹۰۰–۹۶۰) در دو پروژه
  یکی شود تا روی یک دستگاه رفتار مشابه بدهند (اختیاری).
- [ ] **D3. (متوسط) تستِ ریسپانسیوِ خودکار برای ادمین.** کاربرِ وب
  `desktop-layout.mjs` و تست‌های playwright دارد؛ ادمین گاردِ چیدمان ندارد. یک
  تست سبک (مشابه desktop-layout یا اسموک playwright در عرض ۳۹۰px) اضافه شود که
  نبودِ سرریز افقیِ صفحه (`scrollWidth <= clientWidth`) و وجودِ همبرگر را ببندد.
- [ ] **D4. (پایین) بازبینیِ کنترست/خوانایی روی موبایلِ روشن (نور آفتاب)** برای
  متن‌های `--gg-text-faint` در پنل ادمین.

---

## E. ترتیب پیشنهادی ادامهٔ کار

1. ✅ B1 + B2 + B6 (HTTP/2، کش `/ml/`، immutable) — **انجام شد.**
2. ✅ B4 + B5 (preconnect/preload ادمین) و C2 (هدف لمسی) — **انجام شد.**
3. **C1** (صفِ تأیید کارت روی نمای واقعی گوشی) — مهم‌ترین کارِ باقی‌مانده چون
   ادمینِ موبایل ابزار اصلی مدیریت روی گوشی است.
4. **B3** (Brotli) — برد متوسط سرعت، نیاز به نصب ماژول سیستمی با احتیاط.
5. **D3** (تست ریسپانسیو ادمین) تا رگرسیون نگیرد.
6. بقیه (C3–C6، D2/D4) — بهبودی تدریجی.

> یادداشت استقرار: فایلِ زندهٔ nginx روی سرور
> `/etc/nginx/sites-enabled/ghelgheli` است (فایلِ مستقل، نه symlink به
> sites-available) و از نسخهٔ مخزن **جدیدتر** است (upstreamها و
> `snippets/ghelgheli-security-location.conf` دارد). پس کانفیگ مخزن را کورکورانه
> روی سرور کپی نکن؛ تغییر را جراحی روی فایل زنده اعمال کن، همیشه `nginx -t` بگیر
> و بعد reload. بکاپ‌ها را بیرون از `sites-enabled/` نگه‌دار (وگرنه به‌عنوان
> سایت include و upstream تکراری می‌سازند). هر اصلاحِ چیدمانِ وبِ کاربر فقط
> داخل `@media (min-width:…)` باشد تا آینهٔ اندروید نشکند.
